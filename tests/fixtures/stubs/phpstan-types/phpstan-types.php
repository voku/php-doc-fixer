<?php

/**
 * @param resource $stream
 *
 * @return array
 */
function phpstan_type_match(
    string $value,
    callable $callback,
    string $class,
    iterable $items,
    array $map,
    object $payload,
    int $mask,
    $stream
) {
}
